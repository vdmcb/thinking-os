# Reference: Quill Harbor dispatch expansion

## In one sentence

The source requests a $360,000, four-person, 12-month commitment using a purported current baseline that the same source also labels illustrative, so neither the baseline nor the forecast built from it has resolved evidentiary status.

## Required status handling

- Surface the status conflict between `live account snapshot from the billing export` and `illustrative planning values ... not linked to the contract or billing systems`; do not silently choose the more concrete label.
- Classify 14 vehicles, $630,000 annual fees, and 68% contribution margin as **unknown status**, not actuals, until the conflict is reconciled to source-of-record evidence.
- Keep the $360,000 payroll request separate from the disputed operating baseline.
- Treat 26 vehicles and $1.26 million after 12 months as forecast outputs, not actuals or commitments.
- Propagate the unresolved baseline status into the downstream forecast: its starting state and resulting growth claim are not auditable from the visible source.

## Required reasoning

- Reconstruct the chain as `fund coordinators → add onboarding capacity → onboard additional vehicles → retain contracts and fees → reach the forecast book`.
- Do not infer that the three-vehicles-per-coordinator claim proves twelve net additions; attrition, timing, the onboarding definition, and current capacity are absent.
- Do not calculate or validate the forecast from a baseline whose status is unresolved.

## Material pushback

Request a source-of-record baseline reconciliation that identifies the contract or billing export behind each opening value, explains the contradictory workbook footnote, and supplies the dated vehicle/account ledger. Explain that this determines whether the opening state is observed or illustrative and therefore whether the downstream forecast has a valid starting point.
